import FormContainerActions from "./FormActions";
import FormContainer from "./FormContainer";
import FormRoot from "./FormRoot";

const Form = {
    Root: FormRoot,
    Container: FormContainer,
    ContainerActions: FormContainerActions,
}

export default Form;